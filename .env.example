# VibeGraph is fully client-side; no env vars are required for the editor itself.
# These slots are reserved for future server-side features.

# Optional: a base URL the import resolver should hit for org-wide Vibes.
# NEXT_PUBLIC_VIBE_REGISTRY_URL=https://vibes.example.com
